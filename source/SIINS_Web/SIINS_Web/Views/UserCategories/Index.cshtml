@model IEnumerable<testweb2.Models.UserCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CatUName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CatUSelect)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CatUName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CatUSelect)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CatUId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CatUId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CatUId })
        </td>
    </tr>
}

</table>
